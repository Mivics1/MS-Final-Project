# Core (required to reproduce reported results)
# NOTE macOS: xgboost also needs the OpenMP runtime -> brew install libomp
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
xgboost>=2.0

# Document assembly and validation (build/finalise/repaginate/validate scripts)
python-docx>=1.0
Pillow>=10.0
pypdf>=4.0

# Optional extras
# shap>=0.44            # richer feature interpretation (Could-have)
# transformers>=4.40    # transformer baseline (Should-have)
# torch>=2.2            # backend for transformers

# Dev
pytest>=7.0
